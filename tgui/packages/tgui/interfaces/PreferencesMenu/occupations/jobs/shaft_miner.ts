import { Job } from "../base";
import { Proletariat } from "../departments";

const Pioneer: Job = {
  name: "Pioneer",
  description: "Mine ores, sell them. Find artifacts, sell them. \
    Feed your greed. \
    Ensure no one dies of hypothermia.",
  department: Proletariat,
};

export default Pioneer;
