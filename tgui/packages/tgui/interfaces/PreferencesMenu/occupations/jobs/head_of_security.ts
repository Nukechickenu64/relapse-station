import { Job } from "../base";
import { Nobility } from "../departments";

const Coordinator: Job = {
  name: "Coordinator",
  description: "Coordinate your ordinators and ensure the Royal Family's will is \
     being enacted. Commit excessive use of force against anarchists and political dissidents.",
  department: Nobility,
};

export default Coordinator;
