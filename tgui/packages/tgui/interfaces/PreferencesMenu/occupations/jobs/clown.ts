import { Job } from "../base";
import { Proletariat } from "../departments";

const Jester: Job = {
  name: "Jester",
  description: "Go around the outpost, make people laugh and \
    get beat up by the ordinators for trying to cheer them up.",
  department: Proletariat,
};

export default Jester;
