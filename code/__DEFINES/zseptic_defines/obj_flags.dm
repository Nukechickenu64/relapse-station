// Flags for the item_flags var on /obj/item
#define NO_ANGLE_RANDOM_DROP (1<<18) //if dropped, it wont have a randomized turn angle

// ~flags for sharpness in /obj/item
#define SHARP_IMPALING (1<<2)
